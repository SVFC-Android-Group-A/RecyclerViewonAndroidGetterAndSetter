<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecyclerViewonAndroidGetterAndSetter"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity
            android:name=".restaurant.RestaurantPage"
            android:exported="false" />
        <activity
            android:name=".university.UniversityPage"
            android:exported="false" />
        <activity
            android:name=".employee.EmployeePage"
            android:exported="false" />
        <activity
            android:name=".products.ProductsPage"
            android:exported="false" />
        <activity
            android:name=".books.BooksPage"
            android:exported="false" />
    </application>

</manifest>
